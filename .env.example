# Copy to .env.local to route the frontend to the real FastAPI backend.
# When unset (or false), the app uses the fully client-side MockScanner.
VITE_USE_REAL_BACKEND=false
VITE_CLOAK_API_URL=http://localhost:8000
